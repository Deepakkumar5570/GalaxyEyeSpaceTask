MODEL:
  NAME: resnet34_unet

DATASET:
  ROOT: /kaggle/input/datasets/syraverma/galaxeye-change-detection
  PATCH_SIZE: 256
  MODE: concatenated

TRAIN:
  BATCH_SIZE: 16
  EPOCHS: 50
  LR: 0.0001
  NUM_WORKERS: 2

OUTPUT:
  CHECKPOINT_DIR: outputs/checkpoints/resnet34_unet